namespace :petitions do
  desc "Check old petitions"
  # task delete_old: :environment do
  #   # p '=========== starting rake'
  #   # petition = Petition.first
  #   # petition.destroy!
  #   # p '=========== destroyed! id: ' + petition.id.to_s
  #   VotingEndingJob.perform_later
  # end

  # task check: :environment do
  #   VotingEndingJob.perform_later
  # end
end
